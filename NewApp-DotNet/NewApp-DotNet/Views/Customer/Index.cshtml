@model List<Customer>

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-primary">👥 Liste des Clients</h2>
        <a href="http://localhost:5077/Dashboard" class="btn btn-outline-primary shadow-sm">
            ⬅️ Retour au Dashboard
        </a>
    </div>

    <div class="table-responsive rounded shadow">
        <table class="table table-hover table-bordered bg-light-subtle">
            <thead class="table-primary text-center">
                <tr>
                    <th>Nom</th>
                    <th>Email</th>
                    <th>Poste</th>
                    <th>Téléphone</th>
                    <th>Ville</th>
                    <th>Créé le</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var customer in Model)
                {
                    <tr>
                        <td>@customer.name</td>
                        <td>@customer.email</td>
                        <td>@customer.position</td>
                        <td>@customer.phone</td>
                        <td>@customer.city</td>
                        <td>@(customer.createdAt?.ToString("dd/MM/yyyy"))</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
